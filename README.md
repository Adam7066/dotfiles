# Dotfiles

## 使用方法
```bash
git clone git@github.com:Adam7066/dotfiles.git
cd dotfiles
chmod +x install.sh
./install.sh
```

### 進階用法
可選擇只執行部分功能：
- 只安裝 套件：`./install.sh --package`
- 只建立 symlink：`./install.sh --symlink`